1、
manual_labeling_tracks_one_criteria文件夹中每个日期对应的文件夹中都有一个过滤后的汇总所有无人机航迹的“无人机合集“文件夹和规模相等随机抽取的“非无人机合集”。
2、
0116, 0117（以及 0118）
雷达坐标：(32.041850, 118.716537, 80.0, 70.0)
0121（未显式配置，走默认）
雷达坐标：(32.041850, 118.716537, 80.0, 70.0)
0122~0124, 0127
雷达坐标：(39.402443969, 114.156329533, 1026.7118872070, 0.71)
0129, 0131, 0201
雷达坐标：(34.022513, 116.874644, 121.1, 0.1)
0202（未显式配置，走默认）
雷达坐标：(32.041850, 118.716537, 80.0, 70.0)

3、
雷达坐标的四个维度分别是：
第1个：雷达纬度（radar_lat）
第2个：雷达经度（radar_lon）
第3个：雷达所在点的绝对海拔（radar_height，相对海平面）
第4个：雷达天线离地的安装高度（installation_height）

4、
其中dat数据解析会很麻烦，因为手动标注的航迹需要自己去标，这里在脚本代码里标好了，这个脚本也要修改路径才可以运行

5、
归档文件夹中保存的是所有实测的dat文件，包含无人机航迹与其余虚假目标的航迹。

6、
记录了手动标注无人机航迹批号，筛选相关批号的脚本：process_tracks_for_labeling one criteria.py、filter_uav_by_manual_labels.py、check_uav_hit_rate.py

7、
无人机航迹手工标注录入训练流程
    1. 根据gps算法分离标注数据
    src/scripts/converters/rcs_version/process_tracks_for_labeling one criteria.py

    2. filter_uav_by_manual_labels.py（主要脚本）
    路径： src/scripts/analysis/filter_uav_by_manual_labels.py
    作用：
    * 内置 MANUAL_LABELS（日期 → 飞行次数 → 批号列表）
    * 删除不在人工标注列表中的无人机航迹文件
    * 只保留在 MANUAL_LABELS 中出现的批号对应航迹

    3. check_uav_hit_rate.py（命中率核对脚本）
    路径： src/scripts/analysis/check_uav_hit_rate.py
    作用：
    * 同样使用 MANUAL_LABELS
    * 做“手动标注命中率核对与滤除”
    * 结合 GPS 等做更复杂的匹配和统计

