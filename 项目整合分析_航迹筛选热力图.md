# Track-Purification 项目整合分析（面向“航迹筛选热力图”论文扩展）

> 生成时间：2026-04-14  
> 项目路径：`e:\ZCY\AntiGravityProjects\Track-Purification`

## 1. 项目定位与当前目标

本项目的核心是：
- 从雷达 `Track_*.dat` 与无人机 GPS `*.csv` 联合匹配中，筛出疑似无人机航迹；
- 将航迹按“无人机 / 其它目标”落盘为 `Tracks_*.txt`；
- 结合人工标注批号（`MANUAL_LABELS`）进行二次过滤与命中率核查；
- 通过 MATLAB 脚本进行轨迹筛选、可视化与热力图分析。

你后续要做的“航迹筛选热力图论文”，该项目已经具备数据基础与算法雏形，但目前存在多来源、多版本、路径不统一的问题。

---

## 2. 全量资产盘点（系统级）

### 2.1 目录级规模

- 顶层目录：`.vscode`、`manual_labeling_tracks_one_criteria`、`MATLAB`、`ZIP文件`、`归档`
- 顶层脚本：`process_tracks_for_labeling one criteria.py`、`filter_uav_by_manual_labels.py`、`check_uav_hit_rate.py`
- 说明与附件：`说明.md`、`扫描周期表.png`、`Ku监视雷达报文格式(控制与数据)v2.1.xlsx`

### 2.2 文件规模统计（含历史冗余）

- 全项目文件数：`1,193,781`
- 全项目目录数：`946`
- 扩展名主导：`.txt`（约 119 万）

按目录体量（去除 macOS 元数据后）大致为：

- `manual_labeling_tracks_one_criteria`：`596,462` 文件，约 `1125.82 MB`
- `归档`：`116` 文件，约 `1157.08 MB`
- `ZIP文件`：`2` 文件，约 `1086.48 MB`
- `MATLAB`：`11` 文件，约 `0.08 MB`

### 2.3 冗余与来源痕迹

项目含大量 `__MACOSX`、`.DS_Store`、`._*` 元数据副本（来自 macOS 压缩包）。

- `manual_labeling_tracks_one_criteria` 中存在完整镜像冗余：
  - `manual_labeling_tracks_one_criteria/` 正本
  - `__MACOSX/` 元数据镜像
- `ZIP文件` 内部也含 `__MACOSX` 结构。

这会显著增加文件数与检索噪声，但不增加有效研究数据量。

---

## 3. 数据资产结构（研究相关）

## 3.1 `归档/`（原始实验数据仓）

作用：保存实测原始 `dat` + 对应 `csv`（按日期/飞行次组织）。

- 可见日期：`0109, 0112, 0113, 0116, 0117, 0118, 0122, 0123, 0124, 0127, 0129, 0131, 0201, 0202`
- 非冗余统计：`.dat` 约 `66`、`.csv` 约 `50`

说明：并非所有人工标注日期都能在本地 `归档/` 找到（见“缺口清单”）。

## 3.2 `manual_labeling_tracks_one_criteria/`（处理后训练资产）

核心结构：`日期/飞行次/(无人机|其它目标)/Tracks_*.txt`

- 有效日期：`21` 个（`0116~0201` 与 `1208~1228`，含多个离散日期）
- `Tracks_*.txt` 总量（去冗余）约 `596,461`

每个 `Tracks_*.txt` 是单条轨迹样本，典型字段包括：
- `点时间`
- `全速度`、`X向速度`、`Y向速度`、`Z向速度`
- `航向`
- `RCS`
- `距离`、`方位`、`俯仰`
- `经度`、`纬度`、`高度`

文件名模式（从脚本逻辑推断）：
- `Tracks_{批号}_{类别码}_{点数}[...].txt`
- 类别码通常：`1=无人机`, `5=其它目标`

## 3.3 合集目录（已汇总资产）

`说明.md` 提到每个日期有：
- `无人机合集`
- `其他目标合集`

当前实际检测到：
- `无人机合集` 目录：`21` 个，txt 总计约 `990`
- `其他目标合集` 目录：`21` 个，txt 总计约 `1522`

注意：两类“合集”规模并非严格相等，和说明文档描述存在偏差。

---

## 4. 代码体系整合（Python + MATLAB）

## 4.1 Python 三脚本主线

### A) `process_tracks_for_labeling one criteria.py`

职责：
- 读取原始 `Track_*.dat` + GPS `*.csv`
- 按 `targ_num` 分组 + 时间间隔切段
- 通过 GPS 匹配（距离/方位/俯仰/匹配率阈值）分出“无人机/其它目标”
- 输出 `Tracks_*.txt`

关键机制：
- 最短轨迹点数：`MIN_TRACK_LENGTH = 5`
- 切段时间阈值：`SPLIT_TIME_THRESHOLD = 18s`
- 核心匹配阈值来自 `config.gps_matching_thresholds`

雷达站点按日期切换：
- `0116,0117,0118` → `(32.041850,118.716537,80.0,70.0)`
- `0122~0124,0127` → `(39.402443969,114.156329533,1026.7118872070,0.71)`
- `0129,0131,0201` → `(34.022513,116.874644,121.1,0.1)`
- 未配置日期回落默认站点

### B) `filter_uav_by_manual_labels.py`

职责：
- 使用内置 `MANUAL_LABELS`（日期→飞行次→批号列表）
- 删除“无人机目录”中不在人工批号列表的样本
- 保留人工标注目标

### C) `check_uav_hit_rate.py`

职责：
- 基于 `MANUAL_LABELS` 做命中率核查
- 对未命中批号，尝试在“其它目标”中二次 GPS 相似度检索
- 相似度达到阈值可回迁到“无人机”
- 产生命中率日志 `hit_rate_*.log`

配置规模：
- 标注日期：`22`
- 标注飞行次：`125`
- 按飞行去重后的人工批号总数：`704`

## 4.2 MATLAB 路线（算法验证/可视化）

主要文件：
- `Track_DBSCAN.m`：多特征筛选主流程（轨迹切分、插值、密度、DBSCAN、RCS/速度规则、热力图）
- `Main_GPS_Points.m`：点迹读取、周期间分析、SNR 计算、动态图
- `Plot_GPS.m`：GPS 真值轨迹可视化（Range-Azimuth、ENU）
- `Func_ReadTrackData.m` / `Func_ReadPlotData.m`：雷达报文解析
- `Func_GPS_read.m`：GPS 读取与测地转换
- `Radar_para.m`：雷达参数

MATLAB 目前更偏“工程试验与可视化验证”，Python 更偏“批处理数据生产”。

---

## 5. 文档与实现的一致性检查

## 5.1 已确认一致

- `说明.md` 中“3 个主脚本分工”与仓库现状一致
- 雷达坐标分日期策略与 Python 脚本实现一致

## 5.2 已确认不一致 / 缺口

1. 路径硬编码严重（跨机器不可复现）
- Python 中存在 macOS 绝对路径：`/Users/huangyuelin/...`
- MATLAB 中存在 Windows 绝对保存路径：`F:\ZCY\...`

2. 日期覆盖不一致
- `MANUAL_LABELS` 中有 `0121,0202`，但处理目录缺 `0121`，`0202` 也未形成对应日期层
- `manual_labeling_tracks...` 中有 `0118`，但 `MANUAL_LABELS` 未显式给 `0118`

3. 原始数据与标注数据不完全同源
- `归档` 仅覆盖部分日期（偏 01/02 月）
- `12xx` 多日期在处理后数据中存在，但 `归档` 未见对应目录

4. 合集规模与说明偏差
- “无人机合集”和“其他目标合集”当前文件数不对等

5. 工程可执行性缺口
- 仓库不是 git repo（无 `.git`）
- Python 脚本依赖外部模块：`config.gps_matching_thresholds`、`tracks_processing_rcs`（当前仓库未见）

---

## 6. 命中率结果（已有实验产出）

日志文件：
- `hit_rate_20260328_021632.log`
- `hit_rate_20260330_101409.log`

总体汇总（日志末尾）：
- 2026-03-28：`265/704`，命中率 `37.6%`
- 2026-03-30：`239/704`，命中率 `33.9%`

说明：两版结果存在回退，建议将“日志版本 + 代码版本 + 数据快照版本”绑定管理，避免结论漂移。

---

## 7. 面向论文的统一研究主线（建议版）

建议把现有内容重整为 4 层：

1. 数据层（Data Layer）
- `L0` 原始雷达/GPS：`归档/`
- `L1` 轨迹切分样本：`Tracks_*.txt`
- `L2` 标注集：`MANUAL_LABELS` + 修正记录

2. 匹配层（Matching Layer）
- GPS-雷达几何匹配：`距离+方位+俯仰+时间窗`
- 批号轨迹切分策略：时间间隔 + 最短点数

3. 筛选层（Filtering Layer）
- 规则筛选（速度、RCS、密度）
- 聚类筛选（DBSCAN）
- 人工标注闭环修正（命中率回查）

4. 可视化层（Visualization Layer）
- Range-Azimuth 分布
- ENU/X-Y 轨迹
- 空间密度热力图（论文核心图）

---

## 8. “航迹筛选热力图”论文可直接展开的结构

建议论文主问题：
- 如何将“GPS-雷达匹配 + 轨迹筛选规则 + 空间热力图”整合为可量化评估框架？

可写的实验分组：
- Baseline-1：仅阈值匹配
- Baseline-2：阈值 + 人工批号过滤
- Method：阈值 + 密度图 + DBSCAN + 回迁修正

可报告指标：
- 命中率（Hit Rate）
- 误检率（False Alarm）
- 轨迹完整率（Track Completeness）
- 空域覆盖均匀性（Heatmap entropy / density contrast）

可复现实验最小单元：
- 日期 × 飞行次（如 `0123/5`）
- 输入：`Track_*.dat + *.csv + radar config`
- 输出：`无人机/其它目标轨迹 + 热力图 + 指标`

---

## 9. 立刻可执行的项目整治清单

1. 建统一配置文件
- 把所有雷达坐标、路径、阈值迁移到 `config.yaml`

2. 建统一目录规范
- `data/raw`、`data/processed`、`data/labels`、`outputs/figures`

3. 清理冗余资产
- 从研究主分支剔除 `__MACOSX/.DS_Store/._*`
- 保留压缩包但单独归档到 `archive_packages/`

4. 固化版本可追溯
- 每次实验输出附带：代码版本、数据快照、参数哈希

5. 统一 Python 与 MATLAB
- Python 负责批处理和指标
- MATLAB 保留为可视化与算法对照验证

---

## 10. 对你后续研究的直接价值总结

当前项目并不是“不可用”，而是“可研究但未产品化”：
- 优势：数据量大、真实场景多、已有标注与多策略筛选代码
- 瓶颈：路径硬编码、数据版本混杂、流程未统一

一旦完成“配置统一 + 数据清洗 + 流程固化”，你可以直接进入论文写作阶段，重点发力：
- 热力图驱动的轨迹筛选解释性
- 多策略融合带来的命中率与误检权衡
- 跨日期/跨站点泛化表现

